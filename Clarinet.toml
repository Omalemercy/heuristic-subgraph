[project]
name = "heuristic-subgraph"
authors = ["Web3 Developer Team"]
description = "A decentralized registry for metadata tracking and permission management"
telemetry = false
requirements = []
[contracts.heuristic-subgraph-registry]
path = "contracts/heuristic-subgraph-registry.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
